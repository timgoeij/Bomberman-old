import Game from "./Game"
//import io from "socket.io-client"

(function () {

    let init = function()
    {
        let game = new Game();

        //let socket = io();

        //socket.on("test", () =>
        //{

        //});
    };

    window.addEventListener("load", init);

})();
